# Currency-Converter-API
A simple Python Currency Converter that uses an Exchange Rate API to fetch current exchange rates and calculate the converted amount. This project demonstrates the use of the "requests" library, APIs, JSON data, user input, and basic Python calculations.
